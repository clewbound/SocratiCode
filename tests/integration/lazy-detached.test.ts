// SPDX-License-Identifier: AGPL-3.0-only
import { describe, it } from "vitest";

describe.skip("lazy detached-HEAD reconciliation (TODO)", () => {
  it.todo(
    "first query against detached HEAD lazily indexes via sibling-clone (depends on PR 7's sibling-clone path; verify behavior post-merge)",
  );
});
